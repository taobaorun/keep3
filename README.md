# Keep3 release channel

This branch is managed by Keep3's protected release workflows. Signed release
metadata and the Sparkle appcast are published under `release-channel/`.

The standalone Keep3 website is maintained separately and is not deployed from
this branch.
